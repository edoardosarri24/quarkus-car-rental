# container
quarkus.container-image.build=true
quarkus.container-image.push=false
quarkus.container-image.group=edoardosarri
quarkus.container-image.name=first-parallel-service
quarkus.container-image.tag=1.0

# kubernetes
quarkus.kubernetes.name=first-parallel-service
quarkus.kubernetes.deployment-target=kubernetes
quarkus.kubernetes.service-type=NodePort
quarkus.kubernetes.image-pull-policy=Never

# reference
quarkus.http.port=9021
quarkus.http.test-port=9021

# otel-collector
quarkus.otel.service.name=first-parallel-service
quarkus.otel.exporter.otlp.traces.endpoint=http://otel-collector:4317
quarkus.otel.traces.sampler=always_on